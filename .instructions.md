# JsonEditor Copilot Instructions

このファイルは、Copilot が本リポジトリで変更提案を作成する際の制約を定義する。

## 設計不変条件

1. 依存方向は `JsonEditor.App -> JsonEditor.Core` を維持する。
2. `JsonEditor.Core` に WPF 依存を入れない。
3. 検索・置換・JSON検証ロジックは Core 層に集約する。
4. 例外でクラッシュさせず、利用者向け通知は `StatusMessage` 方針を維持する。

## 非機能制約

1. 対象サイズ 10MB 想定の操作性を悪化させない。
2. Regex 処理のタイムアウト設定（250ms）を維持する。
3. 自動保存の最小間隔（5秒）を維持する。

## 変更時の必須対応

1. ロジック変更時はテストを追加または更新する。
2. ユーザー挙動が変わる変更は `doc/User_Manual.md` を更新する。
3. 設計変更は `doc/Design_Specification.md` を更新する。
4. 検証観点が増減する変更は `Verification & Test Design Agent` で検証計画と要件ID対応を先に確定し、`doc/Verification_Spec&result.md` を更新する。
5. 最終的な品質ゲート判定用テスト実行と合否判断は `Quality Gate Agent` の結果を正とする。

## PR作成ルール

1. AI支援PRでは `.github/pull_request_template.md` の「AI変更 検証結果」欄を必ず埋める。
2. AI支援PRでは build/test/coverage の結果を省略しない。
3. AI支援PRではテスト実行日時と coverage artifact path を必ず記載する。
4. 仕様未確定事項は推測実装せず、PR本文に論点として明記する。

## 禁止事項

1. 既存の公開仕様を根拠なく変更しない。
2. 不要な依存パッケージを追加しない。
3. 関連しないファイルをまとめて変更しない。
